name: CI 

on:
  push:
    branches: [main] 
  workflow_dispatch: 

jobs:
 
  build:
    
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run action locally
        run: |
         cd  SiyaaJhawar/demo/.github/workflows/app.yml@main
     
     
      - name: Trigger workflow
        uses: SiyaaJhawar/learning-@main

   
    
    
