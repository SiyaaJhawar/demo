name: Test

on:
  push:
    branches: main
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
       name: checkout code
      -run : npm install
   
      - run: node index.js